# GeoClimate Fetcher Environment Variables

# Optional: Set a custom secret key for authentication tokens
# If not set, a random secret will be generated for each session
# For production deployments, set this to a strong random value
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
GEOCLIMATE_SECRET_KEY=your-secret-key-here
